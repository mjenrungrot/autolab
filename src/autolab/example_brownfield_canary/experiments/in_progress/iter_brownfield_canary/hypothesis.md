# Hypothesis

PrimaryMetric: accuracy; Unit: %; Success: baseline +0.5

If the bootstrap script and environment documentation remain aligned with the
active iteration, operators should be able to reproduce the sanitized run
without fixture-specific drift.
