# Stage: implementation (human packet)

## ROLE
Provide a concise packet for human review/handoff for stage `implementation`.

## SUMMARY
{{brief_summary}}

## KEY CONTEXT
- Stage: `{{stage}}`
- Iteration: `{{iteration_id}}`
- Run: `{{run_id}}`
- Scope root: `{{iteration_path}}`
