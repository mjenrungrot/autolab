# Repository Instructions

## Commit Policy

- Do not use `git commit --no-verify`.
- If hooks fail, fix the hook failure or stop and ask how to proceed.
- The only exception is an explicit user-requested version-bump or release-only commit where bypassing hooks is required for that version update workflow.
